# ascii-luminance-renderer

This project is pushed to: `https://github.com/parkhyokeun00/ascii-ALUMINANCE-MODULATOR-.git`

## Project Structure

- `다중 문자셋 추가 보고서.md`
- `바이너리 텍스트 렌더링에서의 명도 변조 기법.md`
- `전체 큰 그림 (한 장 요약).md`
- `ascii-luminance-renderer@1.0.0`
- `index.html`
- `package-lock.json`
- `package.json`
- `docs/`
- `node_modules/`
- `src/`
    - `atlas.js`
    - `main.js`
    - `processor.js`
    - `renderer.js`
    - `shaders/`
        - `binaryText.frag`
    - `wasm/`
        - `core.cpp`

This project seems to be a JavaScript-based project using `npm` for package management, and likely involves WebAssembly (`wasm/core.cpp`) and WebGL shaders (`shaders/binaryText.frag`) for rendering, possibly related to ASCII art or text rendering with luminance modulation.

To get started, you would typically:
1. `npm install`
2. `npm run dev` (or a similar command if defined in `package.json`)
