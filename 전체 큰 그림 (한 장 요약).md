```

┌───────────────────────────┐
│        Browser (User)      │
│  ───────────────────────  │
│  HTML / CSS (UI)           │
│  ─ Sliders (Contrast etc.) │
│  ─ Image Upload            │
└───────────▲───────────────┘
            │ user input
            │
┌───────────┴───────────────┐
│     JavaScript (Main)      │
│  ─ UI Logic                │
│  ─ Image Loading           │
│  ─ WASM 호출               │
│  ─ WebGL 파라미터 전달     │
└───────▲───────────▲───────┘
        │           │
        │           │ uniforms / buffers
        │           │
┌───────┴───────┐   └───────────────────┐
│   C++ → WASM  │                       │
│ (Compute Core)│                       │
│ ─ Luminance   │                       │
│ ─ Gamma       │                       │
│ ─ Alpha Mask  │                       │
└───────▲───────┘                       │
        │ alpha buffer                  │
        │                               │
┌───────┴───────────────────────────────┴───┐
│          WebGL / Three.js / Shader         │
│  ─ Instanced 0/1 Glyph Rendering           │
│  ─ Alpha-based Visibility                  │
│  ─ GPU Parallel Processing                 │
└────────────────────────────────────────────┘
```


### 파일구조
```
/src
 ├─ index.html
 ├─ main.js
 ├─ renderer.js
 ├─ wasm/
 │   ├─ core.cpp
 │   ├─ core.wasm
 │   └─ core.js
 ├─ shaders/
 │   └─ binaryText.frag
 └─ assets/
     └─ fonts/
```